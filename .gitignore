# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Data (raw ingested JSON is reproducible / not source)
data/raw/*.json

# Cron / run logs
logs/

# Terraform (lock file .terraform.lock.hcl IS committed — pins provider versions)
infra/.terraform/
*.tfstate
*.tfstate.*
infra/terraform.tfvars
crash.log

# OS / editor
.DS_Store
.idea/
.vscode/
